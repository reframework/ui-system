import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Source,
  Description,
} from '@storybook/addon-docs';
import { Menu } from '../../components/Menu';
import * as stories from './Menu.stories.tsx';

<Meta
  title="Menu/Menu"
  component={Menu}
  id="Menu/MDX"
  argTypes={{
    placement: {
      options: [
        'before-before',
        'before-end',
        'before-center',
        'before-start',
        'before-after',
        'end-before',
        'end-end',
        'end-center',
        'end-start',
        'end-after',
        'center-before',
        'center-end',
        'center-center',
        'center-start',
        'center-after',
        'start-before',
        'start-end',
        'start-center',
        'start-start',
        'start-after',
        'after-before',
        'after-end',
        'after-center',
        'after-start',
        'after-after',
      ],
      control: { type: 'select' },
    },
  }}
/>

export const Template = (args) => ({});

# Menu

An accessible dropdown menu for the common dropdown menu button design pattern. Menu uses roving tabIndex for focus management.

## Import

<Source dark language="js" code={`import { Menu } from '@reframework/menu';`} />

## Usage

<br />

<Story name="Intro" story={stories.Intro}>
  {Template.bind({})}
</Story>

<Story name="Source" story={stories.SourceStory} />

## Props

<Story name="ArgsTable" story={stories.ArgsTableStory} />

## Accessing the internal state

To access the internal state of the Menu, use a function as children (commonly known as a render prop).
You'll get access to the internal state isOpen.

<Story name="RenderTrigger" story={stories.RenderTrigger}>
  {Template.bind({})}
</Story>


## Letter Navigation

When focus is on the MenuButton or within the MenuList and you type a letter key, a search begins.
Focus will move to the first MenuItem that starts with the letter you typed.

Coming soon
