export interface Position {
  static: boolean; //	position: static;
  fixed: boolean; //	position: fixed;
  absolute: boolean; //	position: absolute;
  relative: boolean; //	position: relative;
  sticky: boolean; //	position: sticky;
}
